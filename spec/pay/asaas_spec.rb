# frozen_string_literal: true

RSpec.describe Pay::Asaas do
  it "has a version number" do
    expect(Pay::Asaas::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
